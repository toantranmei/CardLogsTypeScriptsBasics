let handleImage: Function

// handleImage = 1
handleImage = (): void => {
    console.log('Image here...')
}

const addImage = (url: string, size: number, author: string|number): number => {
    return 1
}

addImage('https', 1, 1)